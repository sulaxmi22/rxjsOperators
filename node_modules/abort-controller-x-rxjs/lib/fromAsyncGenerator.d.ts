import { Observable } from 'rxjs';
/**
 * Turns abortable async generator to observable.
 */
export declare function fromAsyncGenerator<T>(fn: (signal: AbortSignal) => AsyncIterable<T>): Observable<T>;
